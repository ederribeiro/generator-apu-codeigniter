{
    "name": "<%= _.slugify(authorName) %>/<%= projectSlug %>",
    "version": "0.0.0",
    "authors": [{
        "name": "<%= authorName %>",
        "email": "<%= authorEmail %>"
    }],
    "config": {
        "vendor-dir": "application/third_party"
    },
    "require": {
        "twig/twig": "*"
    }
}
